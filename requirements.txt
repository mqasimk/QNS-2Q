# Exact versions installed in the reference venv (see pyproject.toml, which
# declares these same pins as the package's canonical dependency list -- update
# both together if you deliberately upgrade something). Pinned exactly, not
# with >=, because this repo's job is to reproduce a specific paper's numbers:
# a newer numpy/jax/qutip minor version is exactly the kind of change that
# could silently shift a result well below the "obviously wrong" threshold.
numpy==2.4.0
scipy==1.16.1
matplotlib==3.10.5
jax==0.7.0
jaxlib==0.7.0
qutip==5.2.0
qutip-qip==0.4.1
joblib==1.5.1
pytest==9.0.3

# GPU acceleration (optional, strongly recommended -- trajectories.py batches
# noise shots for a GPU and the Stage-1/3 pipelines are impractically slow on
# CPU alone). Install the CUDA 12 JAX plugin at the SAME version as jax above:
#   pip install jax[cuda12]==0.7.0
# CPU-only machines can skip this; every script still runs, just much slower.
